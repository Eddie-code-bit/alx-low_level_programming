#include "main.h"

/**
 * add - add two numbers
 * @a: the first number
 * @b: the second number
 * Return: returns the value of addition of both numbers
 */

int add(int a, int b)
{
	return (a + b);
}
